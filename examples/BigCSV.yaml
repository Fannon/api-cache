# Example: Cache a big CSV file
title: Big CSV example
description: Loading a bigger CSV file into memory

fetchInterval: 120
timeout: 180
retryDelay: 30

# Use a regular HTTP request
http:
    # And cache the following URL:
    url: https://data.cityofnewyork.us/api/views/5b3a-rs48/rows.csv?accessType=DOWNLOAD
