# GitHub JSON API example

# Use a regular HTTP Request
http:
    # Use the following URL
      url: https://api.github.com/repos/jquery/jquery/issues
      # Query strings can be most conveniently set through an object:
      queryString:
            # Appends ?state=open to the URL
            state: open
